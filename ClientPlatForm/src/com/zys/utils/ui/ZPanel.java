package com.zys.utils.ui;import java.awt.Color;import java.awt.Graphics;import java.awt.Image;import java.awt.LayoutManager;import java.io.File;import java.io.IOException;import javax.imageio.ImageIO;import javax.swing.ImageIcon;import javax.swing.JPanel;public class ZPanel extends JPanel {	private static final long serialVersionUID = 5484559103593318225L;	public ZPanel() {		super();	}	private Image backgroundImage = null;	public ZPanel(boolean isDoubleBuffered) {		super(isDoubleBuffered);	}	public ZPanel(LayoutManager layout, boolean isDoubleBuffered) {		super(layout, isDoubleBuffered);	}	public ZPanel(LayoutManager layout) {		super(layout);	}	private String imageFileName = null;	public String getImageFileName() {		return imageFileName;	}	public void setImageFileName(String imageFileName) {		this.imageFileName = imageFileName;		if (imageFileName != null) {			File imageFile = new File(imageFileName);			if (imageFile != null && imageFile.exists()) {				try {					backgroundImage = ImageIO.read(imageFile);				} catch (IOException e) {					e.printStackTrace();					backgroundImage = null;				}			} else {				backgroundImage = null;			}		} else {			backgroundImage = null;		}	}	public void paintComponent(Graphics g) {		super.paintComponent(g);		setBackground(Color.WHITE);		if (backgroundImage != null) {			// int height = backgroundImage.getHeight(this);			// int width = backgroundImage.getWidth(this);			//			// if (height != -1 && height > getHeight())			// height = getHeight();			//			// if (width != -1 && width > getWidth())			// width = getWidth();			//			// int x = (int) (((double) (getWidth() - width)) / 2.0);			// int y = (int) (((double) (getHeight() - height)) / 2.0);			g.drawImage(backgroundImage, 0, 0, getWidth(), getHeight(), this);		}	}	public void setIcon(ImageIcon appImageIcon) {		if (appImageIcon != null) {			backgroundImage = appImageIcon.getImage();		}	}}